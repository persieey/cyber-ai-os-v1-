# Active session (มี IP/target จริง)
workspace/active/
workspace/outputs/

# challenges/ ถูก track เพื่อเก็บ notes — แต่ยกเว้น binary challenge files ขนาดใหญ่
# (ถ้าต้องการ exclude เพิ่ม ให้เพิ่มที่นี่)


# Personal config (มี LHOST/LPORT ของตัวเอง)
config/tools.yaml

# Python
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Claude Code local settings (permissions are machine-specific)
.claude/settings.local.json
